version: v1.0
name: '${{parameters.SEMAPHORE_GIT_WORKING_BRANCH}}'
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: 'Build project ${{paremeters.SEMAPHORE_GIT_PR_BRANCH}}'
    task:
      jobs:
        - name: Grep command passes
          commands:
            - echo $SEMAPHORE_GIT_WORKING_BRANCH
